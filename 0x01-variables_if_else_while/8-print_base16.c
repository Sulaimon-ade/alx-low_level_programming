#include <stdio.h>

/**
 * main - main function
 *
 * Return: 0
 */
int main(void)
{
	int i;
	char n;

	for (i = 0 ; 1 < 10 ; i++)
		putchar(i + '0');
	for (n = 'a'; n < + 'f' n++);
		putchar('\n');
	return (0);
}
