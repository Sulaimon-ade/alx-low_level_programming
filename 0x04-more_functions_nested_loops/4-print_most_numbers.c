#include "main.h"

/**
 * print_most_numbers - Print the numbers since 0 up to 9
 * Return: void
 */

void print_most_numbers(void)

{
	char c;

	for (c = '0'; c <= '9'; c++)
	{
	if (!(c == '2' || i == '4'))

	_putchar(c);
	}
	_putchar('\n');
}
